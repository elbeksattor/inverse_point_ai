################################################################################
# IP AI Analytics - Demographics (Age/Gender) Secondary GIE Configuration
# InsightFace GenderAge Model - Operates on Face detections from PeopleNet
################################################################################

[property]
gpu-id=0

# Network scaling (InsightFace normalization: (x - 127.5) / 127.5)
net-scale-factor=0.0078431372549
offsets=127.5;127.5;127.5

# Model files
onnx-file=/home/nvidia/projects/inverse_point/ip_ai_analytics/models/demographics/genderage.onnx
model-engine-file=/home/nvidia/projects/inverse_point/ip_ai_analytics/models/demographics/genderage_fp16.engine

# Batch processing
batch-size=16

# Process mode: 2=Secondary (classifier)
process-mode=2

# Model input: 96x96 RGB
model-color-format=0

# Network mode: FP16
network-mode=2

# This is a classifier, not a detector
network-type=1

# Unique ID for this GIE
gie-unique-id=3

# Operate on outputs from PGIE (gie-unique-id=1)
operate-on-gie-id=1

# Only process Face class (class_id=2 from PeopleNet)
operate-on-class-ids=2

# Output tensor name
output-blob-names=fc1

# Output interpretation: age and gender
# fc1 output shape: [1, 3]
# - output[0:2]: gender logits (female, male)
# - output[2]: age / 100
output-tensor-meta=1
